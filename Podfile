# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'sampleCheckIO' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Realm'
  # Pods for sampleCheckIO

  target 'sampleCheckIOTests' do
    inherit! :search_paths
    pod 'Realm/Headers'
    # Pods for testing
  end

  target 'sampleCheckIOUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
